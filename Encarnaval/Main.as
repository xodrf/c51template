package {
	import se.svt.caspar.template.CasparTemplate;
    public class Main extends CasparTemplate {
        public function Main() {
            //var socket:CustomSocket = new CustomSocket("localhost", 5250);
        }
    }
}
